# Word-level transcription. Get a key at elevenlabs.io/app/settings/api-keys
#
# Only needed for `reelforge transcribe`. Everything else — reframing, cutting,
# rendering, linting — runs without it, and a transcript produced elsewhere can
# be dropped into .reelforge/transcripts/<source>.json instead.
ELEVENLABS_API_KEY=
